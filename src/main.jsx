import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import TerminalRegistry from './TerminalRegistry.jsx';import AssignmentHistory from './AssignmentHistory.jsx';import AssignTerminal from './AssignTerminal.jsx';import DailyDispatch from './DailyDispatch.jsx';import AreaDispatch from './AreaDispatch.jsx';import AgentJobs from './AgentJobs.jsx';import AgentManagement from './AgentManagement.jsx';import OfficialImport from './OfficialImport.jsx';import Notifications from './Notifications.jsx';import'./style.css';import'./terminal.css';import'./history.css';import'./operations.css';import'./import.css';import'./notifications.css';import'./agent-management.css';import'./overrides.css';import'./area-dispatch.css';
const nativeFetch=window.fetch.bind(window);window.fetch=async(...args)=>{const response=await nativeFetch(...args);const url=typeof args[0]==='string'?args[0]:args[0]?.url||'';if(response.status===401&&!url.includes('/api/auth/login')){localStorage.removeItem('token');localStorage.removeItem('user');if(!sessionStorage.getItem('sessionResetting')){sessionStorage.setItem('sessionResetting','1');window.location.replace('/')} }return response};sessionStorage.removeItem('sessionResetting');
const API='/api';
async function request(path,options={}){const token=localStorage.getItem('token');const r=await fetch(API+path,{...options,headers:{...(options.body instanceof FormData?{}:{'Content-Type':'application/json'}),...(token?{Authorization:`Bearer ${token}`}:{}) ,...options.headers}});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||'Request failed');return data}
const money=v=>new Intl.NumberFormat('en-CA',{style:'currency',currency:'CAD',maximumFractionDigits:0}).format(v||0);
function Login({done}){const[form,setForm]=useState({email:'admin@example.com',password:'ChangeMe123!'}),[error,setError]=useState('');async function submit(e){e.preventDefault();try{const d=await request('/auth/login',{method:'POST',body:JSON.stringify(form)});localStorage.setItem('token',d.token);localStorage.setItem('user',JSON.stringify(d.user));done(d.user)}catch(e){setError(e.message)}}return <main className="login"><section className="brand-panel"><div className="mark">S</div><div><p className="eyebrow">Smart Access Solutions</p><h1>Every terminal.<br/>Exactly where it belongs.</h1><p>Operational visibility for your complete ATM fleet.</p></div><small>SECURE OPERATIONS PLATFORM · CANADA</small></section><form className="login-card" onSubmit={submit}><div><p className="eyebrow">WELCOME BACK</p><h2>Sign in to Command Center</h2><p className="muted">Use your organization credentials.</p></div><label>Email<input value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<p className="error">{error}</p>}<button>Sign in securely <span>→</span></button><small className="muted">Protected by encrypted authentication and activity monitoring.</small></form></main>}
function Shell(){const[user,setUser]=useState(()=>JSON.parse(localStorage.getItem('user')||'null')),[page,setPage]=useState(()=>user?.role==='agent'?'jobs':'dashboard');if(!user)return <Login done={u=>{setUser(u);setPage(u.role==='agent'?'jobs':'dashboard')}}/>;const logout=()=>{localStorage.clear();setUser(null)};const admin=user.role==='admin',agent=user.role==='agent';const links=agent?[['jobs','My jobs & history','▣']]:[['dashboard','Overview','⌂'],['notifications','Notifications','●'],['terminals','Terminals','▦'],['assign','ATM setup & location','⌖'],['dispatch','Single ATM dispatch','↗'],['area','Area route dispatch','⌘'],['jobs','Agent jobs & history','▣'],...(admin?[['agents','Manage agents','☺'],['history','ATM movement history','◷']]:[]),['import','Official import','⇅']];return <div className="shell"><aside><div className="logo"><div className="mark">S</div><div><b>Smart Access</b><small>COMMAND CENTER</small></div></div><nav>{links.map(x=><button key={x[0]} className={page===x[0]?'active':''} onClick={()=>setPage(x[0])}><i>{x[2]}</i>{x[1]}</button>)}</nav><div className="user"><span>{user.name?.[0]}</span><div><b>{user.name}</b><small>{user.role}</small></div><button onClick={logout}>↪</button></div></aside><section className="content"><header><div><p className="eyebrow">ATM FLEET OPERATIONS</p><h2>{({dashboard:'Command center',notifications:'Notifications & setup queue',terminals:'Terminal registry',assign:'ATM setup & current location',dispatch:'Single ATM dispatch',area:'Location area route dispatch',jobs:agent?'My jobs & completed history':'Agent jobs & assignment history',agents:'Agent management',history:'ATM movement history',import:'Official data import'})[page]}</h2></div><div className="live"><span></span> Systems operational</div></header>{page==='dashboard'?<Dashboard go={setPage}/>:page==='notifications'?<Notifications go={setPage}/>:page==='terminals'?<TerminalRegistry/>:page==='assign'?<AssignTerminal/>:page==='dispatch'?<DailyDispatch done={()=>setPage('jobs')}/>:page==='area'?<AreaDispatch done={()=>setPage('jobs')}/>:page==='jobs'?<AgentJobs role={user.role}/>:page==='agents'?<AgentManagement/>:page==='history'?<AssignmentHistory/>:<OfficialImport/>}</section></div>}
function Dashboard({go}){const[d,setD]=useState(null);useEffect(()=>{request('/dashboard').then(setD)},[]);if(!d)return <Loading/>;return <><div className="hero"><div><p className="eyebrow">MONDAY · FLEET SNAPSHOT</p><h1>Good morning.</h1><p>Here’s what’s happening across your terminal network.</p></div><button onClick={()=>go('assign')}>Assign a terminal <b>→</b></button></div><div className="stats"><Stat label="Total fleet" value={d.total} note="All registered terminals"/><Stat label="Active now" value={d.active} note={`${Math.round(d.active/Math.max(d.total,1)*100)}% of fleet online`} accent/><Stat label="In inventory" value={d.inventory} note="Available for assignment"/><Stat label="Payment alerts" value={d.alerts} note={d.alerts?'Requires attention':'All thresholds healthy'} warn={d.alerts}/></div><div className="grid"><article><div className="article-head"><div><p className="eyebrow">LOCATION INTELLIGENCE</p><h3>Fleet by city</h3></div><button onClick={()=>go('terminals')}>View all →</button></div>{d.cities.map((c,i)=><div className="city"><span className="rank">{String(i+1).padStart(2,'0')}</span><div><b>{c._id||'Unassigned'}</b><small>{money(c.payments)} in payments</small></div><div className="bar"><i style={{width:`${c.count/(d.cities[0]?.count||1)*100}%`}}/></div><strong>{c.count}</strong></div>)}</article><article className="signal"><p className="eyebrow">OPERATIONAL SIGNAL</p><h3>{d.alerts?'Thresholds need attention':'Your fleet is healthy'}</h3><p>{d.active} active terminals are reporting normally. Assignment records and official status remain independently protected.</p><div className="ring"><b>{Math.round(d.active/Math.max(d.total,1)*100)}%</b><small>ACTIVE RATE</small></div></article></div></>}
function Stat({label,value,note,accent,warn}){return <article className={`stat ${accent?'accent':''} ${warn?'warn':''}`}><p>{label}</p><strong>{value}</strong><small>{note}</small></article>}
function Terminals(){const[q,setQ]=useState(''),[data,setData]=useState({items:[]}),[selected,setSelected]=useState(null),[error,setError]=useState('');const load=()=>request('/terminals?limit=100&search='+encodeURIComponent(q)).then(setData).catch(e=>setError(e.message));useEffect(()=>{const t=setTimeout(load,250);return()=>clearTimeout(t)},[q]);async function changeStatus(t,status){const old=t.official.status;setData(d=>({...d,items:d.items.map(x=>x.terminalId===t.terminalId?{...x,official:{...x.official,status}}:x)}));try{await request('/terminals/'+t.terminalId+'/status',{method:'PATCH',body:JSON.stringify({status})})}catch(e){setError(e.message);setData(d=>({...d,items:d.items.map(x=>x.terminalId===t.terminalId?{...x,official:{...x.official,status:old}}:x)}))}}const show=v=>v===0?0:v||'—',date=v=>v?new Date(v).toLocaleString('en-CA'):'—';return <><div className="toolbar"><input placeholder="Search terminal, business, city or address…" value={q} onChange={e=>setQ(e.target.value)}/><span>{data.total||0} terminals</span></div>{error&&<p className="error">{error}</p>}<div className="table-wrap full-table"><table><thead><tr><th>Status</th><th>Terminal ID</th><th>Temp Name</th><th>Name</th><th>Address</th><th>City</th><th>Location Area</th><th>Wish Amount</th><th>Cash Balance</th><th>Cashloading</th><th>Agent</th><th>Notes/Task</th><th>Last Communication</th><th>Last Withdrawal Date</th><th></th></tr></thead><tbody>{data.items.map(t=><tr key={t.terminalId}><td><select className={`status-select ${t.official?.status?.toLowerCase()}`} value={t.official?.status==='Inactive'?'Inactive':'Active'} onChange={e=>changeStatus(t,e.target.value)}><option>Active</option><option>Inactive</option></select></td><td><b>{t.terminalId}</b></td><td>{show(t.official?.tempName)}</td><td>{show(t.official?.name)}</td><td className="wide-cell">{show(t.official?.address)}</td><td>{show(t.official?.city)}</td><td>{show(t.official?.locationArea)}</td><td>{money(t.official?.wishAmount)}</td><td>{money(t.official?.cashBalance)}</td><td>{show(t.official?.cashLoading)}</td><td>{show(t.official?.agent)}</td><td className="wide-cell">{show(t.official?.notesTask)}</td><td>{show(t.official?.lastCommunication)}</td><td>{date(t.official?.lastWithdrawalAt)}</td><td><button className="link" onClick={()=>setSelected(t)}>View →</button></td></tr>)}</tbody></table></div>{selected&&<Drawer t={selected} close={()=>setSelected(null)}/>}</>}
function Drawer({t,close}){return <div className="overlay" onClick={close}><aside className="drawer" onClick={e=>e.stopPropagation()}><button className="close" onClick={close}>×</button><p className="eyebrow">TERMINAL RECORD</p><h2>{t.terminalId}</h2><span className="pill active">{t.official?.status}</span><h4>Original installation</h4><b>{t.original?.businessName||'Not recorded'}</b><p>{t.original?.address}</p><h4>Current assignment</h4><b>{t.current?.businessName||'Unassigned'}</b><p>{t.current?.address}</p><div className="amount"><small>PAYMENT AMOUNT</small><strong>{money(t.current?.paymentAmount)}</strong></div><h4>Assignment history</h4><p className="muted">{t.assignmentHistory?.length||0} recorded movement(s)</p></aside></div>}
function Assign(){const[id,setId]=useState(''),[t,setT]=useState(null),[msg,setMsg]=useState(''),[f,setF]=useState({businessName:'',address:'',city:'',paymentAmount:0,note:'',alertThreshold:0});async function find(e){e.preventDefault();setMsg('');try{setT(await request('/terminals/'+id))}catch(e){setMsg(e.message)}}async function save(e){e.preventDefault();try{await request('/terminals/'+t.terminalId+'/assign',{method:'POST',body:JSON.stringify({...f,paymentAmount:+f.paymentAmount,alertThreshold:+f.alertThreshold})});setMsg('Assignment saved. Original installation details were preserved.');setT(null)}catch(e){setMsg(e.message)}}return <div className="assign-grid"><article><p className="eyebrow">STEP 01 · IDENTIFY</p><h3>Find the machine</h3><form className="search-id" onSubmit={find}><input placeholder="e.g. CA101622" value={id} onChange={e=>setId(e.target.value.toUpperCase())}/><button>Find</button></form>{t&&<div className="original"><small>ORIGINAL INSTALLATION · READ ONLY</small><b>{t.original?.businessName}</b><p>{t.original?.address}</p></div>}{msg&&<p className={msg.startsWith('Assignment')?'success':'error'}>{msg}</p>}</article><article className={!t?'disabled':''}><p className="eyebrow">STEP 02 · NEW ASSIGNMENT</p><h3>Where is it going?</h3><form onSubmit={save}>{[['businessName','Business name'],['address','Street address'],['city','City'],['paymentAmount','Payment amount (CAD)'],['alertThreshold','Balance alert threshold']].map(([k,l])=><label>{l}<input type={k.includes('Amount')||k.includes('Threshold')?'number':'text'} required value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}<label>Assignment note<textarea value={f.note} onChange={e=>setF({...f,note:e.target.value})}/></label><button disabled={!t}>Save assignment <span>→</span></button></form></article></div>}
function Importer(){const[file,setFile]=useState(),[result,setResult]=useState(),[error,setError]=useState('');async function send(){const fd=new FormData();fd.append('file',file);try{setResult(await request('/imports',{method:'POST',body:fd}));setError('')}catch(e){setError(e.message)}}return <div className="import-card"><p className="eyebrow">CONTROLLED SYNCHRONIZATION</p><h3>Import official terminal status</h3><p>The official layer will update. Original locations, current assignments, payments and full movement history will remain untouched.</p><label className="drop"><input type="file" accept=".xls,.xlsx" onChange={e=>setFile(e.target.files[0])}/><b>{file?.name||'Choose Canada Terminal Status file'}</b><small>XLS or XLSX · maximum 10 MB</small></label><button disabled={!file} onClick={send}>Run secure import →</button>{error&&<p className="error">{error}</p>}{result&&<div className="result">{['imported','new','updated','removed','unchanged'].map(k=><div><small>{k}</small><b>{result[k]}</b></div>)}</div>}</div>}
function Loading(){return <div className="loading">Loading operational data…</div>}createRoot(document.getElementById('root')).render(<Shell/>);
